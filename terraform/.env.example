# Environment variables for Terraform
# Copy this file to .env and update with your values
# Usage: source .env (or source terraform/.env from repo root)
# DO NOT commit .env to git!

# =============================================================================
# PROVIDER AUTHENTICATION (Secrets)
# =============================================================================

# Hetzner Cloud API Token
# Get your token from: https://console.hetzner.cloud/
export HCLOUD_TOKEN="replace-me"

# =============================================================================
# SENSITIVE TERRAFORM VARIABLES
# =============================================================================
# These are passed to Terraform via TF_VAR_* environment variables
# because they contain secrets that should not be in tfvars files

# Primary SSH Public Key (required for all exercises)
export TF_VAR_ssh_public_key="ssh-rsa AAAA... replace-me"

# Secondary SSH Public Key (optional)
export TF_VAR_ssh_public_key_secondary=""

# HDM Stuttgart DNS Secret (required for DNS exercises)
# Get this from your dnsupdate.sec file
export TF_VAR_dns_secret="replace-me"

# =============================================================================
# NOTES
# =============================================================================
# - Always keep .env in .gitignore
# - Non-secret config (server_name, location, etc.) goes in config.auto.tfvars
# - Each exercise has a config.auto.tfvars.example template
# - Terraform automatically reads TF_VAR_* environment variables
# - HCLOUD_TOKEN is read directly by the Hetzner Cloud provider
# - HETZNER_DNS_TOKEN is read directly by the Hetzner DNS provider
