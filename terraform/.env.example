# Environment variables for Terraform
# Copy this file to .env and update with your values
# Usage: source .env (or source terraform/.env from repo root)
# DO NOT commit .env to git!

# REQUIRED VARIABLES

# Hetzner Cloud API Token
# Get your token from: https://console.hetzner.cloud/
export HCLOUD_TOKEN="replace-me"

# Primary SSH Public Key
export TF_VAR_ssh_public_key="replace-me"

# OPTIONAL VARIABLES

# Secondary SSH Public Key
export TF_VAR_ssh_public_key_secondary=""
# To use: export TF_VAR_ssh_public_key_secondary="ssh-rsa AAAA..."

# DevOps Username (default: devops)
# The non-root user created via cloud-init
export TF_VAR_devops_username="devops"

# Server Configuration (defaults shown - uncomment to override)
export TF_VAR_server_type="cx23"
export TF_VAR_server_image="debian-13"
export TF_VAR_location="nbg1"

# Server Name (Exercises 13-16 - single server name)
# Used by: base, exercise-14-nginx, exercise-15-cloud-init, exercise-16-known-hosts
export TF_VAR_server_name="server"

# Server Base Name (Exercise 17 - auto-incrementing servers)
# Used by: exercise-17-host-metadata (creates: server-1, server-2, etc.)
export TF_VAR_server_base_name="server"

# Project and Environment Labels
export TF_VAR_project="hello-world"
export TF_VAR_environment="development"

# AVAILABLE LOCATIONS
# nbg1  - Nuremberg, Germany (default)
# fsn1  - Falkenstein, Germany
# hel1  - Helsinki, Finland
# ash   - Ashburn, VA, USA
# hil   - Hillsboro, OR, USA

# NOTES
# - Always keep .env in .gitignore
# - For secrets encryption, see SOPS documentation below
# - Terraform automatically reads TF_VAR_* environment variables
# - HCLOUD_TOKEN is read directly by the Hetzner provider
