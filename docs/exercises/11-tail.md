# 11 - ``tail -f``
- ``tail`` shows last lines of file
- adding ``-f`` flag makes it keep running, continuously showing new lines as they’re added to the file

TLDR: `tail -f logfile` streams new log lines in real time; hit Ctrl+C to stop.
