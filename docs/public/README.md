# Public Assets

Files in this directory are copied to the root of the site during build.

- `.nojekyll` - Tells GitHub Pages not to process the site with Jekyll, which would ignore files starting with underscores (like VitePress assets)
